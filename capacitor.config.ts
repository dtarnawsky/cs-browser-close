import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.browser.close',
  appName: 'cs-browser-close',
  webDir: 'www/browser',
  server: {
    androidScheme: 'https',
  },
};

export default config;
